# Ensure build-time devDependencies (Tailwind/PostCSS/Prisma/TypeScript) are installed on Render.
# Some platforms default to `omit=dev` in production; overriding `omit` keeps dev deps.
omit=optional
